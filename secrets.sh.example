#!/bin/bash
# Copy this file to secrets.sh (gitignored - never committed) and fill in real values.
# run_custard_cream_camera.sh sources secrets.sh automatically if it exists.
# See docs/api-keys.md for where to get each key.

export GOOGLE_API_KEY="your-gemini-api-key"
export FLICKR_API_KEY="your-flickr-api-key"
export FLICKR_API_SECRET="your-flickr-api-secret"
export BSKY_HANDLE="your-handle.bsky.social"
export BSKY_APP_PASSWORD="your-bsky-app-password"
